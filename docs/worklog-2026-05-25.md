# Worklog 2026-05-25

## Summary

Continued the WardFlow v2 Supabase rebuild with a focus on Settings hardening, Supabase write-path confidence, backup snapshot safety, patient deletion policy, and Supabase type-generation readiness.

## Completed

- Split Settings into smaller sections:
  - Backup: `BackupSettings`, `SupabaseBackupSettings`, `LegacyBackupSettings`
  - Admin: `AdminSettings`, `AdminApprovalPanel`, `AdminMemberAccess`, `AdminPatientOwnership`
  - Work: charting copy, schedule categories, calendar colors, AI settings
  - Lab: categories, reference overrides, import settings
- Added Settings UI behavior tests for:
  - Admin member search/filter/role/module/save
  - Patient ownership search/filter/empty states
  - Schedule category add/save
- Added Supabase write-path tests for:
  - Patient fetch/create/update/archive
  - Note, Lab, antibiotic, and schedule add/delete store paths
- Improved Supabase user settings hydration and save status:
  - Store-level normalization for charting, calendar colors, schedule categories, Lab reference overrides
  - Stale save completion protection
  - Visible per-setting save state
- Hardened Supabase backup snapshots:
  - Snapshot create/list/preview/delete UI states
  - Service-level snapshot error formatting
  - Preview input validation and password trimming
  - Snapshot restore impact diff rows comparing snapshot counts against current server counts
- Clarified patient deletion policy:
  - Normal v2 patient deletion is soft archive/list hide only
  - UI now says `환자 숨김 처리`
  - Confirmation explains clinical records remain in Supabase
- Added Supabase type-generation path:
  - `npm run types:supabase`
  - `docs/supabase-types.md`

## Verification

- `npm run type-check` passed.
- Focused Vitest suite passed: 17 files, 57 tests.
- `npm run build` passed.

## Notes

- Supabase CLI is not installed on this machine, so generated Supabase types were not regenerated yet.
- `npm run lint` is still expected to fail until pre-existing legacy lint issues are cleaned up.
- Git status may print `C:\Users\randy/.config/git/ignore: Permission denied`; this did not affect verification.
- A deployed-app smoke test was started after the push, but repo-local config did not include a deployed URL or test account credentials. Browser automation was also unavailable in the session, so the pass remains pending in `docs/supabase-validation.md`.
