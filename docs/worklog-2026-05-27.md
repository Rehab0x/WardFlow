# Worklog 2026-05-27

## Summary

Addressed live v2 usability issues around patient registration/deletion, mobile chart header layout, Lab review, and charting copy/templates.

## Completed

- Patient registration now allows multiple admitted patients to share the same room/bed.
- v2 patient deletion now calls Supabase hard delete instead of archive/list-hide behavior.
- Added `supabase/migrations/202605270001_allow_patient_delete.sql` to allow RLS-protected patient delete for users who can write the patient.
- Mobile workspace header layout now gives C/C and onset text more width by moving action buttons into their own row on small screens.
- Lab tab now loads and renders stored Lab values in a date-by-item table so numeric values remain visible after entry.
- Lab add is now collapsed below the viewing sections, and Culture/Sensitivity results are separated from the numeric Lab value table.
- Charting OCS copy now uses the requested section format with `C/C)`, `Onset)`, `P/I)`, `P/H)`, `Problem List)`, `Plan)`, and `보호자설명)`.
- OCS copy keeps `C/C)` and `Onset)` together while separating the remaining charting sections with blank lines.
- Charting fields in the v2 workspace now expose template buttons backed by the existing template popup/service.
- Medication tab now loads active medication rows and shows separate antibiotic, hospital-medication, and personal-medication sections.
- Antibiotic and hospital/personal medication add forms are collapsed at the bottom of the medication tab.
- Antibiotic form placeholders now read as drug name, dose, and frequency; hospital/personal medication entry now uses drug name, single dose, frequency, timing, and memo without a date field.
- Hospital/personal medication rows now omit the date prefix and expose removal actions, matching the simple add/view/remove workflow.
- Reconnected the existing Lab parser in the v2 Lab tab with paste and XLS modes, saving parsed results by category into the stored Lab result table.
- Reworked hospital/personal medication entry into a two-row form so drug name and memo fields keep usable width, with #1-#4 frequency selection and schedule/timing selectors.
- Reused the existing OCS medication parser in the v2 medication tab so pasted prescriptions can be previewed and saved as hospital or personal medications.
- Expanded #2 medication schedule choices to include bedtime combinations and made the second medication-entry row wrap instead of overflowing narrow panels.
- Charting OCS copy now keeps only `C/C)` and `Onset)` adjacent, adds blank lines before every later section, and uses `P/Hx)` for past history.
- Committed and pushed today's rebuild work as `66bbfc6 Rebuild v2 clinical workflows`.
- Deployed production to Vercel. Alias: `https://ward-flow.vercel.app`; deployment URL: `https://ward-flow-lq20djvsd-randy-kims-projects-6c97c3d7.vercel.app`.

## Verification

- `npm run type-check` passed.
- Focused patient deletion tests passed: 2 files, 6 tests.
- `npm run build` passed.
- `npx vitest run src/stores/usePatientStore.supabase.test.ts src/lib/patientDeletionPolicy.test.ts` passed.
- Vercel production build passed on deployment.

## Notes

- Apply the new Supabase migration before expecting hard delete to work in the deployed project.
