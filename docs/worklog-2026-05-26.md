# Worklog 2026-05-26

## Summary

Continued the WardFlow v2 Supabase rebuild with Settings usability cleanup focused on visible wording in the current Supabase flow.

## Completed

- Cleaned Settings-visible technical wording:
  - Supabase backup snapshot copy now says data counts instead of `record count`.
  - Missing snapshot counts now render as `데이터 개수 없음`.
  - Admin local-mode copy no longer exposes IndexedDB.
  - Lab Import Supabase notice now describes previous local backup sync instead of IndexedDB server-backup sync.
  - Legacy backup copy now says local data instead of Legacy IndexedDB data.
- Updated backup formatter tests to match the new user-facing missing-count label.
- Tightened patient hide/soft-archive wording:
  - The policy help text now starts from `환자 숨김 처리` instead of `환자 삭제`.
  - The v2 patient edit flow and patient store share the same `환자를 숨김 처리하지 못했습니다.` fallback.
  - Supabase patient-store tests now cover archive failure without removing the patient locally.
- Advanced Supabase type-generation readiness:
  - Added Supabase CLI `2.101.0` as a project dev dependency.
  - Verified `npx supabase --version`.
  - Confirmed `supabase link` and remote type generation require `supabase login` or `SUPABASE_ACCESS_TOKEN`.

## Verification

- `npm run type-check` passed.
- Focused Vitest suite passed: 6 files, 26 tests.
- `npm run build` passed.

## Notes

- No obvious mojibake strings were found in the searched Settings/v2 Supabase surface after cleanup.
- `src/types/supabase.ts` was not regenerated because the Supabase CLI has no access token in this local session.
- Existing uncommitted work from the prior Settings accessibility/test pass remains part of the current working tree.
